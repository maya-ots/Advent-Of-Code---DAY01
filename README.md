# Advent-Of-Code---DAY01
The challenge simulates a 100-position dial (0–99) rotated L/R by given amounts. Part 1 asks for the final position after all rotations. Part 2 (method 0x434C49434B) counts every time the dial passes through 0 during rotations, using that count as the password.
